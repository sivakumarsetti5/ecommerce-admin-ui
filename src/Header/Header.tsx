import React from 'react'
import styles from './Header.module.css'
export const Header = () => {
    return (
        <div className={`text-white text-center p-4 ${styles.header}`}>easyCart Admin Portal</div>
    )
}
